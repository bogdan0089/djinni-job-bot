services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      # Keeps the SQLite file across container rebuilds.
      # DATABASE_URL already points here (see Dockerfile).
      - bot-data:/app/data

volumes:
  bot-data:
